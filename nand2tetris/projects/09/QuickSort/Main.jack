class Main{
	function void main(){
	var Arr arr;
	var int n;
	var int i;
	let n=Keyboard.readInt("Size of array: ");
	let arr=Arr.new(n);
	let i = 0;
      while (i < n) {
         let arr[i] = Keyboard.readInt("Enter a number: ");
         let i = i + 1;
      }
	do arr.QuickSort(0,n-1);
	let i = 0;
      while (i < n) {
         do Output.printInt(arr[i]);
		 do Output.println();
         let i = i + 1;
      }
	return;
	}
}